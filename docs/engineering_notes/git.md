
assuming use of "Terminal" on windows or any linux distro

cd into git directory

# Check what branch you are on
git status

# review changes
git diff 

# or use vscode

# stage the changes
git add .

# Commit and add meaningful comment
git commit -m "Add base template and site styling"

# Good commit messages explain what changed.